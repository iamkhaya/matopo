<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Matopo", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= " active" if current_page?(" /providers") %>">
          <%= link_to "Providers", providers_path %>
        </li>
        <li class="<%= " active" if current_page?(" /offerings") %>">
          <%= link_to "Offerings", offerings_path %>
        </li>
        <li class="<%= " active" if current_page?(" /categories") %>">
          <%= link_to "Categories", categories_path %>
        </li>
        <li class="<%= " active" if current_page?(" /activities") %>">
          <%= link_to "Activities", activities_path %>
        </li>
        <li class="<%= " active" if current_page?(" /trips") %>">
          <%= link_to "Trips", trips_path %>
        </li>
        <li class="<%= " active" if current_page?(" /reservations") %>">
          <%= link_to "Reservations", reservations_path %>
        </li>
        <li class="<%= " active" if current_page?(" /users") %>">
          <%= link_to "Users", trips_path %>
        </li>
        <li class="<%= " active" if current_page?(" /users") %>">
          <%= link_to "Reviews", trips_path %>
        </li>

        <% unless user_signed_in? %>
          <li class="<%= " active" if current_page?(" /users/sign_up") %>">
            <%= link_to "Sign up", new_user_registration_path %>
          </li>
          <li class="<%= " active" if current_page?(" /users/sign_in") %>">
            <%= link_to "Sign in", new_user_session_path %>
          </li>
        <% end %>

        <% if user_signed_in? %>
        
          <p class="navbar-text">
          Signed in as <%= current_user.email %>
          </p>

          <li>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
          </li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
